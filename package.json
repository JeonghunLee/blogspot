{
  "name": "ahyuo79-blog-assets",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.23.0",
    "rimraf": "^5.0.0"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "node esbuild.mjs",
    "release": "npm run clean && npm run build"
  }
}